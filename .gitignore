.Rproj.user/
.Rhistory
*.log
outputs/staging/

# Pattern A: data/raw is a materialize cache — only README.md is committed.
# Declared paths live under dataverse.files in replication.yml.
data/raw/*
!data/raw/README.md

# Legacy author dump if present
code/original/
