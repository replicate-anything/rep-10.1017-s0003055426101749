.Rproj.user/
.Rhistory
*.log
outputs/staging/

# Pattern A: data/raw is a materialize cache (gitignored binaries).
# Figure 2 uses Pattern B: baked outputs/10fold_training_results.csv (tracked).
# Declared paths live under dataverse.files in replication.yml.
data/raw/*
!data/raw/README.md

# Legacy author dump if present
code/original/
