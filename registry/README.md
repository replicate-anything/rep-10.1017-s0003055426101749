# Registry sync files

Generated by `replicateEverything::prepare_folder_paper()`.

- Copy `replication.yml` to `registry/studies/10.1017S0003055426101749.yml`
- Merge `index.csv` into the registry repo `index.csv` (replace any existing row with the same DOI).

Or run `replicateEverything::sync_folder_paper()` with `registry_root` pointing at your registry checkout.
